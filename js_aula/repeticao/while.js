var contador = 1; //O contador começa com a primeira vez

while( contador <= 6){ //Enquanto c for menor que 6, vai repetir o texto. 

    console.log(`Passo ${contador}`);
    contador++ // contador = contador +1 (contador recebe contador, mais um)
}